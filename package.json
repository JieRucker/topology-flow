{
  "name": "topology-flow",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "",
    "build": "yarn workspaces run build",
    "docs": "typedoc --out ./docs --target es6 --theme minimal --mode file packages"
  },
  "devDependencies": {
    "copyfiles": "latest",
    "typescript": "~3.8.3",
    "typedoc": "^0.17.7"
  }
}
